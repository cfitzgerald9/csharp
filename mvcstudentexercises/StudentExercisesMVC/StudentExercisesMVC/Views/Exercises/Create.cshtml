@model StudentExercisesMVC.Models.ViewModels.ExerciseCreateViewModel

@{
    ViewData["Title"] = "Create Exercise";
}

<h2>@ViewData["Title"]</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="exercise.exerciseName" class="control-label"></label>
                <input asp-for="exercise.exerciseName" class="form-control" />
                <span asp-validation-for="exercise.exerciseName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="exercise.exerciseLanguage" class="control-label"></label>
                <input asp-for="exercise.exerciseLanguage" class="form-control" />
                <span asp-validation-for="exercise.exerciseLanguage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}